# Adapter: Cline / Roo Code

Ambos consomem regras de projeto em `.clinerules/`. No Roo Code, verifique se a
sua versão usa `.roo/rules/` — o conteúdo dos arquivos é idêntico.

1. Crie `.clinerules/secondmind.md` com o conteúdo completo de `BOOT.md` + as
   variáveis `VAULT` e `SECOND_MIND_PKG` no topo.
2. Na primeira execução, aprove as permissões de escrita no vault.
3. Teste: `evals/golden-session.md`.
