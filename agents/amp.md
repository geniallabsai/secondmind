# Adapter: Amp (Sourcegraph)

Amp é consumidor nativo do padrão `AGENTS.md` (o Sourcegraph participa da
especificação).

1. Coloque na raiz do trabalho: `AGENTS.md` deste pacote + `BOOT.md` inlined
   depois dele (os dois são curtos o bastante).
2. Defina `VAULT` dentro do próprio `AGENTS.md`.
3. Pronto — sem configuração adicional além das permissões de arquivo.
